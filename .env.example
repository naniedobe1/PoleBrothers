# Supabase Configuration
# Get these values from your Supabase Dashboard: Project Settings -> API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Cloudflare Worker URL
# Deploy the Cloudflare Worker first (see cloudflare-worker/README.md)
# Then add the worker URL here
CLOUDFLARE_WORKER_URL=https://your-worker-url.workers.dev

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit the .env file to Git (it's already in .gitignore)
